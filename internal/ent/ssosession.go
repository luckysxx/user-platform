// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/luckysxx/user-platform/internal/ent/ssosession"
	"github.com/luckysxx/user-platform/internal/ent/user"
	"github.com/luckysxx/user-platform/internal/ent/useridentity"
)

// SsoSession is the model entity for the SsoSession schema.
type SsoSession struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 全局登录态令牌哈希，主要承载 Cookie SSO
	SSOTokenHash string `json:"sso_token_hash,omitempty"`
	// 设备标识
	DeviceID *string `json:"device_id,omitempty"`
	// 用户 agent / 客户端标识
	UserAgent *string `json:"user_agent,omitempty"`
	// 客户端 IP
	IP *string `json:"ip,omitempty"`
	// 全局登录态状态
	Status ssosession.Status `json:"status,omitempty"`
	// 当前全局登录态版本
	SSOVersion int64 `json:"sso_version,omitempty"`
	// 创建该全局登录态时的用户全局版本快照
	UserVersion int64 `json:"user_version,omitempty"`
	// 全局登录态过期时间
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// 最近一次心跳时间
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// 撤销时间
	RevokedAt *time.Time `json:"revoked_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SsoSessionQuery when eager-loading is set.
	Edges                      SsoSessionEdges `json:"edges"`
	user_sso_sessions          *int64
	user_identity_sso_sessions *int
	selectValues               sql.SelectValues
}

// SsoSessionEdges holds the relations/edges for other nodes in the graph.
type SsoSessionEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Identity holds the value of the identity edge.
	Identity *UserIdentity `json:"identity,omitempty"`
	// Sessions holds the value of the sessions edge.
	Sessions []*Session `json:"sessions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SsoSessionEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// IdentityOrErr returns the Identity value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SsoSessionEdges) IdentityOrErr() (*UserIdentity, error) {
	if e.Identity != nil {
		return e.Identity, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: useridentity.Label}
	}
	return nil, &NotLoadedError{edge: "identity"}
}

// SessionsOrErr returns the Sessions value or an error if the edge
// was not loaded in eager-loading.
func (e SsoSessionEdges) SessionsOrErr() ([]*Session, error) {
	if e.loadedTypes[2] {
		return e.Sessions, nil
	}
	return nil, &NotLoadedError{edge: "sessions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SsoSession) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ssosession.FieldSSOVersion, ssosession.FieldUserVersion:
			values[i] = new(sql.NullInt64)
		case ssosession.FieldSSOTokenHash, ssosession.FieldDeviceID, ssosession.FieldUserAgent, ssosession.FieldIP, ssosession.FieldStatus:
			values[i] = new(sql.NullString)
		case ssosession.FieldExpiresAt, ssosession.FieldLastSeenAt, ssosession.FieldRevokedAt:
			values[i] = new(sql.NullTime)
		case ssosession.FieldID:
			values[i] = new(uuid.UUID)
		case ssosession.ForeignKeys[0]: // user_sso_sessions
			values[i] = new(sql.NullInt64)
		case ssosession.ForeignKeys[1]: // user_identity_sso_sessions
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SsoSession fields.
func (_m *SsoSession) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ssosession.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case ssosession.FieldSSOTokenHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sso_token_hash", values[i])
			} else if value.Valid {
				_m.SSOTokenHash = value.String
			}
		case ssosession.FieldDeviceID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value.Valid {
				_m.DeviceID = new(string)
				*_m.DeviceID = value.String
			}
		case ssosession.FieldUserAgent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_agent", values[i])
			} else if value.Valid {
				_m.UserAgent = new(string)
				*_m.UserAgent = value.String
			}
		case ssosession.FieldIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ip", values[i])
			} else if value.Valid {
				_m.IP = new(string)
				*_m.IP = value.String
			}
		case ssosession.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = ssosession.Status(value.String)
			}
		case ssosession.FieldSSOVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sso_version", values[i])
			} else if value.Valid {
				_m.SSOVersion = value.Int64
			}
		case ssosession.FieldUserVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_version", values[i])
			} else if value.Valid {
				_m.UserVersion = value.Int64
			}
		case ssosession.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case ssosession.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				_m.LastSeenAt = value.Time
			}
		case ssosession.FieldRevokedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field revoked_at", values[i])
			} else if value.Valid {
				_m.RevokedAt = new(time.Time)
				*_m.RevokedAt = value.Time
			}
		case ssosession.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_sso_sessions", value)
			} else if value.Valid {
				_m.user_sso_sessions = new(int64)
				*_m.user_sso_sessions = int64(value.Int64)
			}
		case ssosession.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_identity_sso_sessions", value)
			} else if value.Valid {
				_m.user_identity_sso_sessions = new(int)
				*_m.user_identity_sso_sessions = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SsoSession.
// This includes values selected through modifiers, order, etc.
func (_m *SsoSession) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SsoSession entity.
func (_m *SsoSession) QueryUser() *UserQuery {
	return NewSsoSessionClient(_m.config).QueryUser(_m)
}

// QueryIdentity queries the "identity" edge of the SsoSession entity.
func (_m *SsoSession) QueryIdentity() *UserIdentityQuery {
	return NewSsoSessionClient(_m.config).QueryIdentity(_m)
}

// QuerySessions queries the "sessions" edge of the SsoSession entity.
func (_m *SsoSession) QuerySessions() *SessionQuery {
	return NewSsoSessionClient(_m.config).QuerySessions(_m)
}

// Update returns a builder for updating this SsoSession.
// Note that you need to call SsoSession.Unwrap() before calling this method if this SsoSession
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SsoSession) Update() *SsoSessionUpdateOne {
	return NewSsoSessionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SsoSession entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SsoSession) Unwrap() *SsoSession {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SsoSession is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SsoSession) String() string {
	var builder strings.Builder
	builder.WriteString("SsoSession(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sso_token_hash=")
	builder.WriteString(_m.SSOTokenHash)
	builder.WriteString(", ")
	if v := _m.DeviceID; v != nil {
		builder.WriteString("device_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.UserAgent; v != nil {
		builder.WriteString("user_agent=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.IP; v != nil {
		builder.WriteString("ip=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("sso_version=")
	builder.WriteString(fmt.Sprintf("%v", _m.SSOVersion))
	builder.WriteString(", ")
	builder.WriteString("user_version=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserVersion))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(_m.LastSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RevokedAt; v != nil {
		builder.WriteString("revoked_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// SsoSessions is a parsable slice of SsoSession.
type SsoSessions []*SsoSession
