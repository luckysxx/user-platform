# If empty, frontend requests /api/* and relies on Vite proxy in local dev.
# In production, set this to your API base URL, for example:
# VITE_API_BASE_URL=https://sso-api.yourcompany.com
VITE_API_BASE_URL=
